<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@color/backgroundGradient">

    <!-- Title Section -->
    <TextView
        android:id="@+id/textViewSummary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Enrollment Summary"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/title_color"
        android:gravity="center"
        android:paddingBottom="16dp" />

    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@color/divider_color"
        android:layout_marginBottom="16dp" />

    <!-- Subjects Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/section_background"
        android:padding="16dp"
        android:layout_marginBottom="16dp"
        android:elevation="4dp">

        <!-- Outer LinearLayout for Subject Title -->
        <TextView
            android:id="@+id/subject1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Subjects:"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/section_title_color"
            android:paddingBottom="8dp" />

        <!-- Inner LinearLayout for TextView Subjects -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/textViewSubjects"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text=""
                android:textSize="14sp"
                android:textColor="@color/black"
                android:paddingBottom="8dp" />
        </LinearLayout>

    </LinearLayout>

    <!-- Total Credits Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/section_background"
        android:padding="16dp"
        android:elevation="4dp">

        <!-- Total Credits Label -->
        <TextView
            android:id="@+id/credits1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Total Credits:"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/section_title_color"
            android:paddingBottom="8dp" />

        <!-- Nested LinearLayout for Credits Result -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Display Credits Result -->
            <TextView
                android:id="@+id/textViewTotalCredits"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textSize="16sp"
                android:textColor="@color/black"
                android:paddingEnd="8dp"
                android:layout_marginEnd="8dp" />

            <!-- Optional: You can add additional elements in this LinearLayout, like an icon -->
        </LinearLayout>

    </LinearLayout>

</LinearLayout>